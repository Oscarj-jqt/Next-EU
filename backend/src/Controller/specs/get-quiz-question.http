### Get a quiz question
GET http://localhost:8080/api/get-quiz-question

> {%
    client.test("It should return a successful status code", function() {
        client.assert(response.status === 200, "Response status is not 200");
    });
%}
